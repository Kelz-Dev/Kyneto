# Kyneto Sidecar - Python Dependencies (CPU-only, lightweight)
# =============================================================

pandas>=1.5.0,<3.1
numpy>=1.21.0,<2.5
scikit-learn>=1.0.0,<1.9
flask>=2.0,<4.0
prometheus-flask-instrumentator
xgboost==2.0.3

# Database (for live data ingestion + feedback loop)
psycopg2-binary>=2.9.0
