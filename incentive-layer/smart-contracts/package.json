{
    "name": "@incentive-layer/smart-contracts",
    "version": "0.1.0",
    "description": "Smart contracts for decentralized storage marketplace on Polygon",
    "author": "Your Name",
    "license": "MIT",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:mumbai": "hardhat run scripts/deploy.js --network mumbai",
        "deploy:polygon": "hardhat run scripts/deploy.js --network polygon",
        "clean": "hardhat clean"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "hardhat": "^2.17.0",
        "@types/node": "^18.0.0",
        "typescript": "^5.0.0",
        "ts-node": "^10.9.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "ethers": "^6.7.0"
    }
}